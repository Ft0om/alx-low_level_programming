#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * Discription: alphabets expect q and e
 * Retun: 0
 */
int main(void)
{
	char ce = 'a':
	
	while (ce <= 'a')
	{
		if (ce != 'q' || 'e')
		putchar(ce);
		ce++;
	}
	putchar('\n');
	return (0);
}
